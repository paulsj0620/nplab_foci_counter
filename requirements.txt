# foci_counter — Python environment (verified on Python 3.12, Apple Silicon).
# Recreate with:
#   conda create -n focicnt python=3.12 -y
#   conda activate focicnt
#   pip install -r requirements.txt
#
# StarDist/TensorFlow can be version-finicky on Apple Silicon; if TF fails to
# install, try the latest tensorflow and matching stardist.

numpy==2.5.0
scipy==1.18.0
scikit-image==0.26.0
scikit-learn==1.9.0
czifile==2026.6.12
imagecodecs==2026.6.26
tensorflow==2.21.0
stardist==0.9.2
csbdeep==0.8.2
openpyxl==3.1.5
pillow==12.3.0
