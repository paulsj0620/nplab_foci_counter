# foci_counter — Python deps (minimum versions; verified on Python 3.12).
#
# IMPORTANT: create a Python 3.12 env FIRST, then install into it:
#   conda create -n focicnt python=3.12 -y
#   conda activate focicnt
#   which python        # must point inside envs/focicnt
#
# Local Mac (CPU):
#   pip install -r requirements.txt
#
# HPC with NVIDIA GPU (CUDA) — prefer conda for the base stack, pip for the rest;
# do NOT rely on exact pins across OS/arch. See slurm/SETUP_HPC.md:
#   conda install -c conda-forge numpy scipy scikit-image scikit-learn \
#       openpyxl pillow imagecodecs czifile
#   pip install "tensorflow[and-cuda]" stardist csbdeep

numpy>=2.0
scipy>=1.13
scikit-image>=0.24
scikit-learn>=1.5
czifile>=2024.5.22
imagecodecs>=2024.1.1
tensorflow>=2.16
stardist>=0.9.1
csbdeep>=0.8.0
openpyxl>=3.1
pillow>=10.0
