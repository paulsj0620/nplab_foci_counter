# foci_counter

H&E 염색 CZI (Zeiss AxioScan) 이미지에서 간 조직의 **inflammation foci** 개수를
자동으로 세는 도구.

## 상태
초기 개발 중. 모듈식 배치 CLI 파이프라인으로 구성 예정.

## 데이터
`dataset/` 폴더의 원본 CZI 파일(수십 GB)은 GitHub에 올리지 않습니다(`.gitignore` 처리).
코드와 설정만 버전 관리합니다.
